
#include <string.h>
#include "../mnemo/mnemo.h"


describe "mne_tos()"
{
  context "birds are flying"
  {
    it "finds the コンビニ convenient"
    {
      ensure(1 == 1);
    }
    it "does not care about \n"
    {
      ensure(1 == 2);
    }

    // that's all folks
  }
}

